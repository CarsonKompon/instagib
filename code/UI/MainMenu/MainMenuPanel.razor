@using System;
@using Sandbox;
@using Sandbox.UI;
@attribute [StyleSheet]

@namespace Instagib
@inherits Sandbox.UI.NavHostPanel

<root>
    <div class="navigator-canvas" slot="navigator-canvas"></div>
</root>

@code
{
    public Action<long> OnExit { get; set; } = delegate { };

    public MainMenuPanel()
    {
        DefaultUrl = "/";

        AddDestination("/", typeof(MainMenuFrontPage));
        AddDestination("/play", typeof(MainMenuPlayPage));
        AddDestination("/create-server", typeof(MainMenuCreateServerPage));
        @* AddDestination("/songs", typeof(SongSelectPage));
AddDestination("/osu", typeof(OsuPage));
AddDestination("/addons", typeof(AddonPage));
AddDestination("/game", typeof(GamePage));
AddDestination("/settings", typeof(SettingsPage)); *@
    }
}